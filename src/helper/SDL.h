#pragma once

#include <SDL.h>

void SDL_RenderDrawCircle(SDL_Renderer* const renderer, const int x, const int y, const int radius);
void SDL_RenderFillCircle(SDL_Renderer* const renderer, const int x, const int y, const int radius);